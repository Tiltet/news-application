import { StyleSheet } from 'react-native';

export const commentsStyle = StyleSheet.create({
    container: {

    },
    comment: {
        marginTop: 10,
    },
    comment_bottom: {
        marginTop: 2,
    }
})
